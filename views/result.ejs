<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>Resume Analysis Result</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-800 text-white min-h-screen flex items-center justify-center p-6">

    <div class="w-full max-w-4xl bg-zinc-900 rounded-2xl shadow-xl p-8 space-y-8">

        
        <div class="text-center">
            <h1 class="text-3xl font-bold">Resume Analysis Report</h1>
            <p class="text-zinc-400 mt-2">ATS compatibility & improvement insights</p>
        </div>

        
        <div class="bg-zinc-800 p-6 rounded-xl">
            <div class="flex justify-between items-center mb-2">
                <h2 class="text-xl font-semibold">ATS Match Score</h2>
                <span class="text-3xl font-bold 
                    <%= analysis.matchPercentage < 40 ? 'text-red-400' :
                        analysis.matchPercentage < 70 ? 'text-yellow-400' :
                        'text-green-400' %>">
                    <%= analysis.matchPercentage %>%
                </span>
            </div>

            <div class="w-full bg-zinc-700 rounded-full h-3">
                <div class="h-3 rounded-full
                    <%= analysis.matchPercentage < 40 ? 'bg-red-500' :
                        analysis.matchPercentage < 70 ? 'bg-yellow-400' :
                        'bg-green-500' %>"
                    style="width: <%= analysis.matchPercentage %>">
                </div>
            </div>
        </div>

        
        <div class="bg-zinc-800 p-6 rounded-xl">
            <h2 class="text-xl font-semibold mb-4"> Missing Skills</h2>
            <div class="flex flex-wrap gap-3">
                <% analysis.missingSkills.forEach(skill => { %>
                    <span class="bg-red-600/20 text-red-400 px-4 py-2 rounded-full text-sm">
                        <%= skill %>
                    </span>
                <% }) %>
            </div>
        </div>

     
        <div class="bg-zinc-800 p-6 rounded-xl">
            <h2 class="text-xl font-semibold mb-4">🛠 Recommended Improvements</h2>
            <ul class="space-y-3 list-disc list-inside text-zinc-300">
                <% analysis.improvements.forEach(item => { %>
                    <li><%= item %></li>
                <% }) %>
            </ul>
        </div>

     
        <div class="bg-zinc-800 p-6 rounded-xl">
            <h2 class="text-xl font-semibold mb-4">Suggested Resume Bullet Points</h2>
            <ul class="space-y-3 list-disc list-inside text-zinc-300">
                <% analysis.editedSuggestions.forEach(edit => { %>
                    <li><%= edit %></li>
                <% }) %>
            </ul>
        </div>

        <div class="text-center text-zinc-400 text-sm pt-4 border-t border-zinc-700">
            Made with ❤️ by Anosh Singh
        </div>

    </div>

</body>
</html>
