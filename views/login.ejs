<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="bg-zinc-800 text-white p-5 flex flex-col  justify-center items-center w-full h-screen">
        <h1 class="text-3xl font-bold">Login</h1>
        <div class="w-1/4 mt-5">
            <% if(error){  %>
                <p><%= error%></p>
                <% } %>
            <form action="/login" method="post" class="flex flex-col">
                <input type="text" name="email" placeholder="Enter Your Email" class="p-3 outline-none bg-zinc-600 mt-5 rounded-md" required>
                <input type="password" name="password" placeholder="Enter Your Password" class="accent-white p-3 outline-none bg-zinc-600 mt-5 rounded-md" required>
                <a href="/signup" class="mt-5 text-yellow-500 text-sm">I am new to the page</a>
                <input type="submit" class="p-3 bg-blue-500 rounded-md mt-5" value="Login">

            </form>
        </div>
    </div>
</body>
</html>